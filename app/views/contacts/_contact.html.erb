
<%= turbo_frame_tag dom_id(contact) do %>
<div class="mb-3 mr-3">
  <div class = "contact contact-body" %>
    <div class=" p-6 rounded-md bg-gray-300">
      <div class="flex justify-between mb-2 items-center">
        <p class="text-lg font-semibold items-center"><%= contact.name %></p>
      </div>
      <div>
        <%= render_email_if_exists(contact) %></p> 
        <% contact.phones.each do |phone| %>
        <div class="flex mt-3 text-sm">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-7 w-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
            d="M16 8l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2M5 3a2 2 0 00-2 2v1c0 8.284 6.716 15 15 15h1a2 2 0 002-2v-3.28a1 1 0 00-.684-.948l-4.493-1.498a1 1 0 00-1.21.502l-1.13 2.257a11.042 11.042 0 01-5.516-5.517l2.257-1.128a1 1 0 00.502-1.21L9.228 3.683A1 1 0 008.279 3H5z" />
          </svg>
            <p class="text-md self-center"><%= phone.number %> </p>
            <%= render_default_if_true(phone) %>
        </div>
        <% end %>
        <div class="flex mt-2 space-x-2">
          <span class="text-md px-6 py-2 rounded-md bg-green-500 text-indigo-50 font-semibold cursor-pointer" text-indigo-50 font-semibold cursor-pointer><%= link_to "Edit", edit_user_contact_path(contact.user_id, contact)%></span>
          <span class="text-md px-6 py-2 rounded-md bg-red-500 text-indigo-50 font-semibold cursor-pointer"><%= link_to "Delete", user_contact_path(:user_id => contact.user_id, :id => contact), method: :delete, remote: true%></span>
        </div>
      </div>
    </div>
  </div>
</div>
<% end %>  

